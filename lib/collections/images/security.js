Images.allow({
  insert(userId, imageObj) {
    return true;
  },
  update(userId, imageObj) {
    return true;
  },
  remove(userId, imageObj) {
    return true;
  },
  download(userId, imageObj) {
    return true;
  }
});
